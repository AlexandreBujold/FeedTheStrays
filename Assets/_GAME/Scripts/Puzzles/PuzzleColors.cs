using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PuzzleColors
{
    RED,
    GREEN,
    BLUE,
    YELLOW,
    PURPLE,
    LengthOperator //Being the final value, acts as the length of the enum. Can be used to determine length for Random.Range due to method being exclusive for integers
}
